{"slug":"euler-number","title":"Euler's Number","description":"Write a programme to compute the value of Euler's Number","tags":["java","js","py","jl","c","cpp","go"],"contributors":["LevBernstein","Anzo52","mathcodes","ssavi-ict","yogeshCt3","umaxyon","SanjayShetty01"],"latestUpdateDate":"2022-07-09T07:24:10Z","contentHtml":"<h2>Write a programme to compute the value of Euler's Number</h2>\n<pre><code>1 + 1/1! + 1/2! + 1/3! + ... + 1/n!\n</code></pre>\n<hr>\n<pre><code># write a programme to compute the Euler Number\n\nfunction euler(n)\n    values = 1\n    for i in [1:1:n;]\n\n        value = (1 / factorial(big(i)))\n        value = Float64(value, RoundUp)\n        values += value\n    end\n\n    return round(values, digits=3)\nend\n\nn = 50\nprintln(\"Input  : \", n)\nprintln(\"Output : \", euler(n))\n\n</code></pre>\n<pre><code>// program to compute the value of Euler's number\nimport java.util.Scanner;\n\npublic class EulersNumber {\n\n  public static double eulersNumber(int n) {\n    double e = 1;\n    double factorial = 1;\n\n    for (int i = 1; i &#x3C;= n; i++) {\n      factorial = factorial * i;\n      e = e + 1 / factorial;\n    }\n\n    return e;\n  }\n\n  public static void main(String[] args) {\n    Scanner input = new Scanner(System.in);\n    System.out.print(\"Input  : \");\n    int n = input.nextInt();\n    System.out.println(\"Output : \" + eulersNumber(n));\n    input.close();\n  }\n}\n\n</code></pre>\n<pre><code>//Javascript program to compute\n// sum of series 1/0! + 1/1! + 1/2! + .. + 1/n!\n\n// function calculating the factorials for the sum of series calculated in the following function\nconst factorial = (n) => {\n  let r = 1;\n  for (let i = 2; i &#x3C;= n; i++) r *= i;\n  return r;\n};\n// `sum` is a lot like function for the \"harmonic series\" solution, but here on line 17 we call the function 'factorial' in the denominator as needed.\nconst sum = (n) => {\n  let s = 0;\n  for (let i = 0; i &#x3C;= n; i++) s += 1.0 / factorial(i);\n  return s;\n};\n\nconsole.log(\"n : 50\");\nconsole.log(\"\\nsum : \", sum(50));\n\n</code></pre>\n<pre><code># Write a programme to compute the value of Euler's Number\nfrom math import factorial\n\n\ndef e(n):\n    return sum((1 / factorial(i) for i in range(0, n + 1)))\n\n\nprint(e(50))\n\n</code></pre>\n<pre><code>// Write a programme to compute the value of Euler's Number\n\npackage main\n\nimport (\n\t\"fmt\"\n)\n\nfunc factorial(n uint64) uint64 {\n\t// returns the factorial of a given number\n\tvar factorial_num uint64 = 1\n\tvar i uint64\n\tfor i = 1; i &#x3C;= n; i++ {\n\t\tfactorial_num *= i\n\t}\n\treturn factorial_num\n}\n\nfunc main() {\n\tvar n uint64\n\tvar i uint64\n\tvar float_value_at_i float64\n\tvar euiler_number float64 = 1\n\tfmt.Printf(\"Enter Value of N : \")\n\tfmt.Scan(&#x26;n)\n\n\tfor i = 1; i &#x3C;= n; i++ {\n\t\tfloat_value_at_i = 1.0 / float64(factorial(i))\n\t\teuiler_number += float_value_at_i\n\t}\n\tfmt.Printf(\"\\nThe Euler number is : \")\n\tfmt.Println(euiler_number)\n}\n\n</code></pre>\n<pre><code>// Write a programme to compute the value of Euler's Number\n#include &#x3C;iostream>\n\nusing namespace std;\n\nint main()\n{\n    int N;\n    cout &#x3C;&#x3C; \"Enter N : \";\n    cin >> N;\n    float eNumber = 1, fact = 1;\n    for (int i = 1; i &#x3C;= N; i++)\n    {\n        fact = fact * i;\n        eNumber += (1.0 / fact);\n    }\n    cout &#x3C;&#x3C; \"Euler Number of \" &#x3C;&#x3C; N &#x3C;&#x3C; \" : \" &#x3C;&#x3C; eNumber &#x3C;&#x3C; endl;\n}\n\n</code></pre>\n<pre><code>// Write a programme to compute the value of Euler's Number\n#include &#x3C;stdio.h>\n\nint main()\n{\n    int N;\n    printf(\"Enter N : \");\n    scanf(\"%d\", &#x26;N);\n    float eNumber = 1, fact = 1;\n    for (int i = 1; i &#x3C;= N; i++)\n    {\n        fact = fact * i;\n        eNumber += (1.0 / fact);\n    }\n    printf(\"Euler Number of %d : %f\\n\", N, eNumber);\n}\n\n</code></pre>\n","markdown":"\n## Write a programme to compute the value of Euler's Number\n\n```txt\n1 + 1/1! + 1/2! + 1/3! + ... + 1/n!\n```\n\n---\n\n\n<CodeBlock>\n        \n```julia\n# write a programme to compute the Euler Number\n\nfunction euler(n)\n    values = 1\n    for i in [1:1:n;]\n\n        value = (1 / factorial(big(i)))\n        value = Float64(value, RoundUp)\n        values += value\n    end\n\n    return round(values, digits=3)\nend\n\nn = 50\nprintln(\"Input  : \", n)\nprintln(\"Output : \", euler(n))\n\n```\n                \n```java\n// program to compute the value of Euler's number\nimport java.util.Scanner;\n\npublic class EulersNumber {\n\n  public static double eulersNumber(int n) {\n    double e = 1;\n    double factorial = 1;\n\n    for (int i = 1; i <= n; i++) {\n      factorial = factorial * i;\n      e = e + 1 / factorial;\n    }\n\n    return e;\n  }\n\n  public static void main(String[] args) {\n    Scanner input = new Scanner(System.in);\n    System.out.print(\"Input  : \");\n    int n = input.nextInt();\n    System.out.println(\"Output : \" + eulersNumber(n));\n    input.close();\n  }\n}\n\n```\n                \n```js\n//Javascript program to compute\n// sum of series 1/0! + 1/1! + 1/2! + .. + 1/n!\n\n// function calculating the factorials for the sum of series calculated in the following function\nconst factorial = (n) => {\n  let r = 1;\n  for (let i = 2; i <= n; i++) r *= i;\n  return r;\n};\n// `sum` is a lot like function for the \"harmonic series\" solution, but here on line 17 we call the function 'factorial' in the denominator as needed.\nconst sum = (n) => {\n  let s = 0;\n  for (let i = 0; i <= n; i++) s += 1.0 / factorial(i);\n  return s;\n};\n\nconsole.log(\"n : 50\");\nconsole.log(\"\\nsum : \", sum(50));\n\n```\n                \n```python\n# Write a programme to compute the value of Euler's Number\nfrom math import factorial\n\n\ndef e(n):\n    return sum((1 / factorial(i) for i in range(0, n + 1)))\n\n\nprint(e(50))\n\n```\n                \n```go\n// Write a programme to compute the value of Euler's Number\n\npackage main\n\nimport (\n\t\"fmt\"\n)\n\nfunc factorial(n uint64) uint64 {\n\t// returns the factorial of a given number\n\tvar factorial_num uint64 = 1\n\tvar i uint64\n\tfor i = 1; i <= n; i++ {\n\t\tfactorial_num *= i\n\t}\n\treturn factorial_num\n}\n\nfunc main() {\n\tvar n uint64\n\tvar i uint64\n\tvar float_value_at_i float64\n\tvar euiler_number float64 = 1\n\tfmt.Printf(\"Enter Value of N : \")\n\tfmt.Scan(&n)\n\n\tfor i = 1; i <= n; i++ {\n\t\tfloat_value_at_i = 1.0 / float64(factorial(i))\n\t\teuiler_number += float_value_at_i\n\t}\n\tfmt.Printf(\"\\nThe Euler number is : \")\n\tfmt.Println(euiler_number)\n}\n\n```\n                \n```cpp\n// Write a programme to compute the value of Euler's Number\n#include <iostream>\n\nusing namespace std;\n\nint main()\n{\n    int N;\n    cout << \"Enter N : \";\n    cin >> N;\n    float eNumber = 1, fact = 1;\n    for (int i = 1; i <= N; i++)\n    {\n        fact = fact * i;\n        eNumber += (1.0 / fact);\n    }\n    cout << \"Euler Number of \" << N << \" : \" << eNumber << endl;\n}\n\n```\n                \n```c\n// Write a programme to compute the value of Euler's Number\n#include <stdio.h>\n\nint main()\n{\n    int N;\n    printf(\"Enter N : \");\n    scanf(\"%d\", &N);\n    float eNumber = 1, fact = 1;\n    for (int i = 1; i <= N; i++)\n    {\n        fact = fact * i;\n        eNumber += (1.0 / fact);\n    }\n    printf(\"Euler Number of %d : %f\\n\", N, eNumber);\n}\n\n```\n                \n</CodeBlock>\n          \n        "}